steps:
- id: 'update discord bot vm'
  name: 'gcr.io/google.com/cloudsdktool/cloud-sdk'
  entrypoint: 'sh'
  args:
  - '-c'
  - |
      PROJECT_ID=$(gcloud config list --format 'value(core.project)' 2>/dev/null)
      gcloud builds submit --tag gcr.io/"${PROJECT_ID}"/listener
      gcloud compute instances update-container archive-listener --zone=europe-west4-c --container-image=gcr.io/spartan-theorem-328817/listener:latest
